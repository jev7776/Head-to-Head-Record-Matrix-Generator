# Head-to-Head-Record-Matrix-Generator
This repository contains a simple Node.js script that reads a JSON file of team win loss records vs opponents and generates a formatted head to head matrix table. The solution demonstrates working with nested data structures, loops and basic logic to transform raw sports data into a readable table, similar to Sports Reference head-to-head summaries
